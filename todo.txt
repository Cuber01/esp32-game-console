X connect jtag
X fix issues related to map size
X check if snake-on-snake collision works
X fix crash after a few minutes: memory corruption / stack overflow suspected: check cyclic buffer
X fix issue where apple does not reappear
- fix apples spawning on snake
- do rendering on antoher thread or find other way to take input without it getting delayed by delay()
- write driver
- remove arduino dependencies
- add sound
- order better buttons
- figure out how to minimize everything and fit into a case
- polish the game:
    ~ multiple apples
    X make it impossible for snake head to move into its neck itself
    ~ screen wrap
    - game over screen & restart

Driver info:
- we use 4-line SPI (hardwired by producer)

4-line SPI pins used:
CSX (CS): Chip selection signal
WRX (SCL): Clock
DCX (DC): Data is regarded as a command when WRX is low. Data is regarded as a parameter or data when WRX is high
SDA (SDA): Serial input/output data

WRX and DCX might need to be swapped, but it seems like a datasheet error that confused the two

Transmission via 4-line serial interface: chapter 8.4 of datasheet

their init:
static const uint8_t PROGMEM
  generic_st7789[] =  {                // Init commands for 7789 screens
    9,                              //  9 commands in list:
    ST77XX_SWRESET,   ST_CMD_DELAY, //  1: Software reset, no args, w/delay
      150,                          //     ~150 ms delay
    ST77XX_SLPOUT ,   ST_CMD_DELAY, //  2: Out of sleep mode, no args, w/delay
      10,                          //      10 ms delay
    ST77XX_COLMOD , 1+ST_CMD_DELAY, //  3: Set color mode, 1 arg + delay:
      0x55,                         //     16-bit color
      10,                           //     10 ms delay
    ST77XX_MADCTL , 1,              //  4: Mem access ctrl (directions), 1 arg:
      0x08,                         //     Row/col addr, bottom-top refresh
    ST77XX_CASET  , 4,              //  5: Column addr set, 4 args, no delay:
      0x00,
      0,        //     XSTART = 0
      0,
      240,  //     XEND = 240
    ST77XX_RASET  , 4,              //  6: Row addr set, 4 args, no delay:
      0x00,
      0,             //     YSTART = 0
      320>>8,
      320&0xFF,  //     YEND = 320
    ST77XX_INVON  ,   ST_CMD_DELAY,  //  7: hack
      10,
    ST77XX_NORON  ,   ST_CMD_DELAY, //  8: Normal display on, no args, w/delay
      10,                           //     10 ms delay
    ST77XX_DISPON ,   ST_CMD_DELAY, //  9: Main screen turn on, no args, delay
      10 };                          //    10 ms delay

things i need to add:
SWRESET
MADCTL?
ST77XX_INVON
NORON